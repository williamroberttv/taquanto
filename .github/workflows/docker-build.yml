# Disabled: retained as documentation for the previous Docker/Oracle deployment.
#
# name: Build and Push Docker Image
#
# on:
#   push:
#     branches:
#       - main
#
# env:
#   REGISTRY: ghcr.io
#   IMAGE_NAME: ${{ github.repository }}
#
# jobs:
#   docker:
#     runs-on: ubuntu-latest
#
#     permissions:
#       contents: read
#       packages: write
#
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#
#       - name: Login no GitHub Container Registry
#         uses: docker/login-action@v3
#         with:
#           registry: ${{ env.REGISTRY }}
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}
#
#       - name: Build e Push da imagem
#         uses: docker/build-push-action@v6
#         with:
#           context: .
#           file: ./ci/prod/Dockerfile
#           push: true
#           tags: |
#             ghcr.io/${{ github.repository }}:latest
#             ghcr.io/${{ github.repository }}:${{ github.sha }}
#
#   deploy:
#     name: Deploy na Oracle
#     runs-on: ubuntu-latest
#     needs: docker
#     environment: production
#     timeout-minutes: 10
#
#     concurrency:
#       group: production-deploy
#       cancel-in-progress: false
#
#     steps:
#       - name: Configurar acesso SSH
#         env:
#           SSH_PRIVATE_KEY: ${{ secrets.ORACLE_SSH_KEY }}
#           SSH_KNOWN_HOSTS: ${{ secrets.ORACLE_KNOWN_HOSTS }}
#         run: |
#           install -m 700 -d ~/.ssh
#
#           printf '%s\n' "$SSH_PRIVATE_KEY" > ~/.ssh/oracle
#           chmod 600 ~/.ssh/oracle
#
#           printf '%s\n' "$SSH_KNOWN_HOSTS" > ~/.ssh/known_hosts
#           chmod 600 ~/.ssh/known_hosts
#
#       - name: Executar deploy
#         env:
#           SSH_HOST: ${{ secrets.ORACLE_HOST }}
#           SSH_USER: ${{ secrets.ORACLE_USER }}
#         run: |
#           ssh \
#             -i ~/.ssh/oracle \
#             -o IdentitiesOnly=yes \
#             -o StrictHostKeyChecking=yes \
#             -o ServerAliveInterval=30 \
#             -o ConnectTimeout=15 \
#             "$SSH_USER@$SSH_HOST" << 'REMOTE_SCRIPT'
#               set -Eeuo pipefail
#
#               cd /opt/taquanto
#
#               echo "Baixando imagens..."
#               docker compose pull
#
#               echo "Atualizando aplicação..."
#               docker compose up -d --remove-orphans
#
#               echo "Containers em execução:"
#               docker compose ps
#
#               echo "Removendo imagens antigas..."
#               docker image prune -f
#           REMOTE_SCRIPT
